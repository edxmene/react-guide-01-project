import React from "react";

const Button = () => {
  return (
    <div>
      <button type="submit" className="btn btn-danger mt-2">
        Add User
      </button>
    </div>
  );
};

export default Button;
